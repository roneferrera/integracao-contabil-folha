# ============================================================
# requirements.txt
# Integração Contábil Domínio V6.5
# ============================================================

# Interface web
streamlit>=1.40.0

# Leitura de PDF
pdfplumber>=0.11.0

# Manipulação de dados
pandas>=2.2.0

# Excel — leitura e escrita de .xlsx
openpyxl>=3.1.5

# Excel — leitura de .xls (formato legado)
# ATENÇÃO: xlrd 2.x suporta APENAS .xls, não .xlsx
xlrd>=2.0.2

# Fuzzy matching — fallback nas funções _e_* de detecção de rubrica
rapidfuzz>=3.9.0
